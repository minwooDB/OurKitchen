CREATE TABLE START_UP (
	id INT NOT NULL auto_increment PRIMARY KEY,
   	signgucd INT NOT NULL,
   	signgunm INT NOT NULL,
   	danger VARCHAR(20) NOT NULL,
   	close float NOT NULL,
   	remain_term float NOT NULL,
   	plma float NOT NULL
)